{
  "name": "api-learning-roadmap",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@8.15.4",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
